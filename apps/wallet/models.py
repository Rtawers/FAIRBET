from django.db import models  # noqa: F401

# Modelos: LedgerEntry, Account.
# Cuentas del ledger: wallet_usuario, casa, apuestas_pendientes, bonos.
# Saldo NUNCA se almacena: SUM(credits) - SUM(debits).
