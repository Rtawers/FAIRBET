# Bitacora semanal (individual)

| Semana | Que hice | Bloqueos | Decisiones |
|--------|----------|----------|------------|
| Semana 3 (26-05-2026) | Endpoints REST de betting (apostar, cash-out, listar), dashboard operador (GGR, exposure, volumen y CSV MINCETUR), logica rollover 5x derivada excluyendo apuestas CANCELLED, liquidacion de goleador exacto con caso SIN_GOLEADOR, adaptacion de tests al patron self.user.profile.save(). | Integracion del rollover depende de cuenta BONUS (wallet/Lennart). Integracion del goleador depende del mercado creado en events (Lucrecia). | GGR calculado solo sobre SETTLEMENT. Rollover derivado (no almacenado). Goleador usa seleccion especial SIN_GOLEADOR para empate tecnico 0-0. |

| Semana 3 (26-05-2026) | Implementación de reglas anti-fraude (Velocidad y Varianza >300%), creación del modelo inmutable SuspiciousActivity, aplicación estricta del ciclo TDD (Fases RED y GREEN), y redacción técnica de ADR-0013, ADR-0014 y el Informe de Cumplimiento (Ley 31557). | Restricción de arquitectura para operar en "solo lectura" sobre los modelos de Wallet (LedgerEntry). Error del ORM de Django (FieldError) al intentar ordenar el historial por fecha. Conflicto de dependencias (whitenoise) al sincronizar con main. | El motor anti-fraude se diseñó como un observador pasivo que registra alertas inmutables sin bloquear transacciones en caliente para no romper otros módulos (ADR-0014). Se ordenó el historial por -id para optimizar consultas y evitar fallos del ORM. |



# Bitácora Semanal de Desarrollo (Individual)
**Desarrollador Responsable:** Maicol

| Semana | Qué hice | Bloqueos | Decisiones |
| :--- | :--- | :--- | :--- |
| **Semana 2 (23-05-2026)** | Implementación de las bases del modelo de cuentas concurrentes, testing inicial de paralelismo e hilos sobre carteras de usuario. Redacción de ADRs 0001, 0002 y 0003. | Bloqueo técnico debido a fallos masivos en hilos secundarios utilizando `django.test.TestCase` (`Account.DoesNotExist`). | Se migró la suite de pruebas a `TransactionTestCase` para permitir que múltiples hilos concurrentes visualicen estados commiteados reales en base de datos. |
| **Semana 3 (26-05-2026)** | Endpoints REST de betting (apostar, cash-out, listar), dashboard operador (GGR, exposure, volumen y CSV MINCETUR), lógica rollover 5x derivada excluyendo apuestas CANCELLED, liquidación de goleador exacto con caso SIN_GOLEADOR, adaptación de tests al patrón `self.user.profile.save()`. | Integración del rollover depende de cuenta BONUS (wallet/Lennart). Integración del goleador depende del mercado creado en events (Lucrecia). | GGR calculado solo sobre SETTLEMENT. Rollover derivado (no almacenado en tabla fija). Goleador usa selección especial SIN_GOLEADOR para empate técnico 0-0. |
| **Semana 3 (26-05-2026)** | Implementación de reglas anti-fraude (Velocidad y Varianza >300%), creación del modelo inmutable SuspiciousActivity, aplicación estricta del ciclo TDD (Fases RED y GREEN), y redacción técnica de ADR-0013, ADR-0014 y el Informe de Cumplimiento (Ley 31557). | Restricción de arquitectura para operar en "solo lectura" sobre los modelos de Wallet (LedgerEntry). Error del ORM de Django (FieldError) al intentar ordenar el historial por fecha. Conflicto de dependencias (whitenoise) al sincronizar con main. | El motor anti-fraude se diseñó como un observador pasivo que registra alertas inmutables sin bloquear transacciones en caliente para no romper otros módulos (ADR-0014). Se ordenó el historial por -id para optimizar consultas y evitar fallos del ORM. |
| **Semana 4 (28-05-2026)** | Implementación del endpoint de registro `/api/accounts/register/` con inicialización inmutable de `UserProfile.kyc_status` en `PENDING_VERIFICATION`. Creación del servicio `registrar_usuario_kyc` integrado con el endpoint `/api/accounts/kyc/`. Codificación del algoritmo matemático Módulo 11 para la validación del dígito verificador del DNI peruano. Implementación del control anti-fraude multi-cuenta por IP, forzando estado `BLOCKED` y guardando la bitácora inmutable en el modelo `SuspiciousActivity`. Redacción del ADR de diseño de ciclo de vida del usuario. | Conflicto técnico severo por la señal `post_save` duplicada que creaba una cuenta de Wallet adicional e inválida durante la ejecución de los tests unitarios concurrentes. El cálculo exacto de la mayoría de edad arrojaba falsos negativos al evaluar el día exacto de cumpleaños en servidores con zonas horarias desincronizadas. | Las validaciones críticas se diseñaron de manera "atómica" dentro del servicio, interrumpiendo el flujo con excepciones `ValueError` antes de que los datos toquen el ORM o la BD. El control multi-cuenta por IP se configuró para interceptar en el cuarto registro consecutivo desde la misma dirección `REMOTE_ADDR`, forzando el estado a `BLOCKED` y disparando de forma inmutable la alerta del trigger de seguridad. |